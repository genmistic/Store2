<!-- VARIABLES: base_estado -->
<div class="card">
  <div class="card-body">
    <%= form_with(model: [:admin, base_estado]) do |form| %>
        
        <div class="mb-3">
            <%= form.label :name, class: 'form-label' %>
            <%= form.text_field :name, class: 'form-control' %>
            <% if base_estado.errors[:name].any? %>
              <div>
                <%= base_estado.errors[:na].first %>
              </div>
            <% end %>
        </div>

        <%= form.submit class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>

